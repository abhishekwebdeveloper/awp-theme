# AWP Theme

Custom WordPress theme.

## Getting started

1. `pnpm run boot`: install Composer and PNPM dependencies and build. Husky pre-commit hook auto-installs via `pnpm install`'s `prepare` script.
2. Copy `.env.local.example` to `.env.local` and fill in your values.
